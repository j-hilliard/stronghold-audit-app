# Improver Report — 2026-04-17

Generated by the `improver` agent. Research only — no changes made.

---

## Section 1: Competitive Intelligence

### Sources Researched
- SafetyCulture / iAuditor (Capterra, G2, GetApp, Apple App Store reviews)
- Cority (cority.com/corityone/audit-inspections, GoAudits comparison)
- Intelex (Capterra comparison, field1st.com)
- VelocityEHS (softwareadvice.com, field1st.com alternatives)

---

### Feature Gap Table

| # | Gap Feature | Competitor(s) | Priority | Notes |
|---|-------------|--------------|----------|-------|
| 1 | **Offline / airplane-mode audit capture** | SafetyCulture, BasinCheck, GoAudits | **HIGH** | iAuditor supports offline but with sync bugs; BasinCheck has ECDSA-signed offline ops. Stronghold has no offline mode at all — field crews cannot complete audits without connectivity. |
| 2 | **Native mobile app (iOS + Android)** | SafetyCulture, VelocityEHS, Cority, GoAudits | **HIGH** | All four competitors ship native or PWA mobile apps. Stronghold is browser-only. Field auditors use phones on job sites. |
| 3 | **GPS / geolocation tagging on audit submissions** | GoAudits, SafetyCulture | **HIGH** | Geo-tagging proves where the inspection happened — critical for multi-site construction/utility companies. |
| 4 | **QR code / barcode scanning to link audits to assets or locations** | SafetyCulture, GoAudits | **MEDIUM** | Scaning a tag on a piece of equipment opens the right template and pre-fills the header. Speeds up field use significantly. |
| 5 | **Pre-built regulatory compliance template library** | SafetyCulture (100k+ templates), Intelex, VelocityEHS | **MEDIUM** | Competitors ship OSHA, ISO 45001, NFPA, and industry-specific templates out of the box. Stronghold requires admins to build every template from scratch. |
| 6 | **AI-powered predictive risk / trend flagging** | Cority (embedded AI), VelocityEHS (ML risk forecasting) | **MEDIUM** | Stronghold has AI narrative summaries (Claude Haiku) but not predictive risk scoring across audit history. |
| 7 | **Multi-site / multi-division executive dashboard with cross-org benchmarking** | SafetyCulture enterprise, Cority, Intelex | **MEDIUM** | Competitors let an EHS director see all sites ranked by score on one screen and benchmark site vs site. Stronghold shows per-division data only. |
| 8 | **Contractor / third-party audit workflows** | Cority, Intelex, VelocityEHS | **MEDIUM** | Auditing subs and monitoring their compliance separately from internal crews. Cority specifically calls out contractor compliance monitoring. |
| 9 | **Training integration / linkage (finding → mandatory training assignment)** | Intelex (full EHS stack), VelocityEHS | **MEDIUM** | When a non-conformance is found, these platforms can automatically assign a training module. Stronghold has no training module. |
| 10 | **Two-way corrective action portal (assignee-facing external link)** | SafetyCulture, GoAudits | **MEDIUM** | Assignees who are not system users receive a tokenised link, update the CA status, upload evidence, without needing a full account. Stronghold CAs require a user login. |
| 11 | **Regulatory / compliance register with automatic applicability rules** | Cority (full compliance management module), Intelex, VelocityEHS | **LOW** | Tracks which regulations apply per site/jurisdiction and alerts on changes. Overkill for Stronghold's current scope but a stated Cority differentiator. |
| 12 | **White-label / tenant branding per division** | GoAudits, SafetyCulture enterprise | **LOW** | Custom logo and colors per customer/division in enterprise tier. |

---

## Section 2: Azure + Entity Framework Audit

### 2.1 Program.cs

| Check | Finding | Severity |
|-------|---------|---------|
| `MigrateAsync()` called at startup? | **No.** Synchronous `context.Database.Migrate()` is called at startup in Local/Dev environments. In Production the migration call is **skipped** — only `DbInitializer.Initialize(context, true)` runs. There is no migration applied on Production startup. | **CRITICAL** — Migrations will not run automatically on Azure deployment unless applied out-of-band. |
| Connection strings hardcoded? | `tenantId` and `clientId` are hard-coded as fallback strings at lines 19–20 (`78d53608...`, `619f5cca...`). They are overwritten in Dev/Prod environments but fall through in "Local". More critically the NSwag Swagger UI OAuth2 authorize URL (line 201) has the tenant GUID **burned in** unconditionally. | **ADVISORY** — TenantId/ClientId should come exclusively from config; hard-coded GUIDs in NSwag config will break if the tenant changes. |
| Health check registered? | **No** `AddHealthChecks()` / `MapHealthChecks()` found anywhere in Program.cs. | **ADVISORY** — Azure App Service and Container Apps use `/health` or `/healthz` probes to determine instance readiness. Without one, the platform cannot distinguish a cold-starting app from a crashed one. |
| Logging to stdout configured? | In non-Local environments (Dev, Prod) the default ASP.NET Core logging pipeline is used without explicit `ClearProviders()` / `AddConsole()`. In Local, `ClearProviders()` + `AddConsole()` IS set. In Production on Azure App Service, stdout logging is off by default and the EventLog sink is irrelevant. | **ADVISORY** — Add `builder.Logging.AddConsole()` unconditionally (or confirm Application Insights captures all logs in Production). |
| `EnableSensitiveDataLogging()` in Production? | Present inside `#if DEBUG` block (line 230) — correctly guarded. | OK |
| HTTPS redirect | Skipped for Local (`if (!isLocal) app.UseHttpsRedirection()`). Correct behavior. HSTS applied for non-Local. | OK |
| CORS | `AllowAnyOrigin().AllowAnyMethod().AllowAnyHeader()` applied globally with no environment guard. | **CRITICAL** — This is a wildcard CORS policy that permits any origin to call the API. In Production on Azure this allows cross-site requests from any website on the internet. Must be locked to known origins (e.g. the webapp domain) in Production. |

### 2.2 AppDbContext.cs

| Check | Finding | Severity |
|-------|---------|---------|
| DbContext registered as Scoped? | **No** — `AddDbContextFactory<AppDbContext>` is used (lines 218, 233), not `AddDbContext`. Factory registration is `Singleton` by default. Individual `AppDbContext` instances created by the factory are Scoped/transient per-factory-use. This is intentional and valid for the factory pattern, but means the context itself is not DI-injected as Scoped. | OK (intentional factory pattern) |
| `EnableRetryOnFailure()` configured? | Conditionally enabled: `if (!isLocal) providerOptions.EnableRetryOnFailure()` — correctly skipped for Local dev (Docker SQL sometimes mis-triggers retries). | OK |
| `EnsureCreated` usage? | Not found in any production code. Only a documentation reference found in `docs/database/migrations-log.md`. | OK |
| `AsNoTracking` usage? | Found in 5 domain files with 12 total usages. Presence is good. However, coverage audit of read-only list queries (e.g., `GetCorrectiveActions`, `GetRepeatFindings`) should be checked — only those 5 files use it. Many other read-heavy handlers (template lists, division lists, audit history) may be loading tracked entities unnecessarily. | **ADVISORY** — Extend `AsNoTracking()` to all read-only query handlers that do not mutate returned entities. |

### 2.3 appsettings Files

| Check | Finding | Severity |
|-------|---------|---------|
| `appsettings.json` | Empty `{}` — no secrets. | OK |
| `appsettings.Local.json` | Contains SQL Server password `Str0nghold@Audit2026!` and Azure AD TenantId/ClientId in plaintext. | **CRITICAL** — This file must NEVER be committed to source control. Verify it is in `.gitignore`. If it has been committed historically, rotate the SQL password and treat it as compromised. |
| `appsettings.Production.json` | Does not exist. | OK (Production config comes from Azure App Configuration via managed identity — correct pattern.) |

### 2.4 Migrations

| Check | Finding | Severity |
|-------|---------|---------|
| Migration count | **20 migration files** (excluding Designer.cs and Snapshot). 3 were added on 2026-04-17 alone (Phase2A, Phase2C). Growth is rapid. | **ADVISORY** — At ~20 migrations the count is still manageable. Plan to bundle into an EF Migration Bundle before Azure deployment to avoid startup-time migration at scale. The 3 newest migrations (Phase2A_AutoCa, Phase2A_RequirePhotoOnNc, Phase2C_DivisionAuditFrequency) lack Designer.cs snapshot files, which may indicate they were generated without `dotnet ef migrations add` (hand-authored). Verify they are in the EF snapshot. |

### 2.5 Summary of Issues by Severity

**CRITICAL (must fix before Azure Production deployment):**
1. No `MigrateAsync()` in Production path — migrations never run on Azure.
2. Wildcard CORS (`AllowAnyOrigin`) applied globally — must be restricted to known origins in Production.
3. `appsettings.Local.json` contains SQL password and Azure AD credentials in plaintext — must be in `.gitignore` and secrets rotated if ever committed.

**ADVISORY (best practice, fix before GA):**
4. No health check endpoint — Azure probes will not work correctly.
5. Stdout logging not explicitly configured for Production — Application Insights may silently drop logs.
6. Hard-coded `tenantId`/`clientId` GUID strings at top of Program.cs and in NSwag OAuth2 URL.
7. `AsNoTracking()` not applied to all read-only query handlers — risk of unnecessary change tracking overhead at scale.
8. 3 newest migration files appear hand-authored without `dotnet ef` tooling — verify EF snapshot is current.

---

## Section 3: Decisions Needed From User

1. **Offline / Mobile** — Do you want a PWA (Progressive Web App) approach (ship a service worker to the Vue app) or a full native mobile build? PWA is faster to ship; native is better UX offline.
2. **CORS** — What is the Production webapp domain so CORS can be locked to it? (e.g. `https://audit.thestrongholdcompanies.com`).
3. **Migration strategy for Azure** — Should migrations be applied via the startup path (requires DB migration call added to Production branch) or via a CI/CD pre-deploy step (`dotnet ef database update` or EF bundles)?
4. **Health check** — Add a basic `/health` endpoint (returns 200) or a full check (probes SQL Server connectivity)?
5. **appsettings.Local.json** — Confirm this file is in `.gitignore`. If not, provide confirmation so the SQL password and AD secrets can be rotated safely.
6. **Competitor template library** — Should the app seed OSHA/ISO 45001 template starters, or is this out of scope for the current phase?

---

*End of report.*
