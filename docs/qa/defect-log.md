# Defect Log

| Defect ID | Date | Severity | Area | Summary | Repro Steps | Expected | Actual | Evidence Links | Assigned To | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| DEF-0001 | 2026-03-31 | High | Audit module - New Audit page | Division cards render blank and all appear selected | 1. Open `New Audit` page 2. Observe division grid without interacting, then click any card once | One selected card only; each card displays valid division `code` and `name` | Many cards render with blank labels and all cards show `Selected` state | User-provided screenshot in thread (2026-03-31) | Claude | Open |
| DEF-0002 | 2026-04-01 | High | Audit module - Sidebar navigation stability | Pages become blank after switching routes while an audit is in progress | 1. Start new audit 2. Open `New Audit`/`Reports`/`Audits` in repeated sequence from sidebar 3. Repeat several cycles without refresh | Route changes always render page content and stay interactive | Intermittent blank page until browser refresh | `webapp/tests/e2e/audit-navigation-stability.spec.ts` + `docs/qa/defect-handoffs/2026-04-01-audit-nav-template-reporting.md` | Claude | Open |
| DEF-0003 | 2026-04-01 | Medium | Template Manager | Required template-admin controls are missing | 1. Set `PW_AUDIT_TEMPLATE_GATE=true` 2. Run `npm run test:e2e:audit:template-gate` | Template admin supports clone version, add question, drag/drop reorder, publish | UI is placeholder only; required buttons/selectors not present | `webapp/tests/e2e/audit-template-admin-contract.spec.ts` + `docs/qa/defect-handoffs/2026-04-01-audit-nav-template-reporting.md` | Claude | Open |
| DEF-0004 | 2026-04-01 | Medium | Reports and KPI | Required KPI/reporting controls are missing | 1. Set `PW_AUDIT_REPORTING_GATE=true` 2. Run `npm run test:e2e:audit:reporting-gate` | Reports page exposes filters, KPI cards, scoped rows, and export action | UI is placeholder only; required filters/selectors not present | `webapp/tests/e2e/audit-kpi-reporting-contract.spec.ts` + `docs/qa/defect-handoffs/2026-04-01-audit-nav-template-reporting.md` | Claude | Open |
| DEF-0005 | 2026-04-01 | Medium | Template Manager data contract | Template version selector renders undefined label | 1. Run `PW_AUDIT_TEMPLATE_GATE=true` template gate 2. Open failure screenshot | Template rows should show valid template/version labels from API contract | UI shows `undefined` for template display text, indicating response mapping mismatch | `webapp/test-results/artifacts/audit-template-admin-contr-201c5-op-reorder-and-publish-flow-chromium/test-failed-1.png` | Claude | Open |
| DEF-0006 | 2026-04-01 | High | QA coverage gap (resolved) | Mock-only audit tests did not fail on live API 500/blank-screen states | 1. Break live `/v1/divisions` or `/v1/audits` 2. Run previous mock-only suite | QA should fail before user sees blank screens | Mock suite passed because endpoints were mocked | Added `audit-live-api-guard.spec.ts` and `audit-live-blank-screen-guard.spec.ts` | QA | Closed |
| DEF-0007 | 2026-04-01 | High | Runtime environment / API availability | Audit API intermittently unavailable on `localhost:7221`, causing proxy `500` and blank audit screens | 1. Run `npm run qa:baseline` 2. Observe `audit-live-api-guard` and `audit-live-blank-screen-guard` failures 3. See Vite proxy `ECONNREFUSED` for `/v1/divisions` and `/v1/audits` | API reachable and returns `200` JSON for audit bootstrap endpoints | Endpoint unreachable (`ECONNREFUSED`) or proxied `500`, resulting in failed loads and blank pages | Baseline run output + `webapp/tests/e2e/audit-live-api-guard.spec.ts` + `webapp/tests/e2e/audit-live-blank-screen-guard.spec.ts` | Claude | Open |
| DEF-0008 | 2026-04-06 | High | Incident management - employee persistence | Adding a second employee to an existing incident fails save with `409 Conflict`; employee is not persisted | 1. Open existing incident with one employee 2. Click `Add Employee` and populate new row 3. Click `Save Changes` | Save succeeds (`202`), incident reload shows both employees, DB row count increments | Save returns `409`; API throws `DbUpdateConcurrencyException`; DB still shows original employee count | API log `PUT /v1/IncidentReport/{id} -> 409`, SQL update batch in `api-5045-local.out.log`, and incident employee count query in `safety.incident_employee_involved` | Claude | Open |
| DEF-0009 | 2026-04-09 | Medium | Report Composer - newsletter template bootstrap | Composer load issues `GET /v1/audits/newsletter-template?divisionId={id}` and receives `404`, causing console error noise and failing live QA gate | 1. Run `PW_REQUIRE_AUDIT_API=true` live composer test 2. Open `/audit-management/reports/composer` 3. Generate/save draft | Composer bootstrap should not produce API 4xx during normal flow when no template exists | Endpoint returns `404` (twice per load) for missing template; live gate fails on API errors | `webapp/tests/e2e/audit-report-composer-live-manage-drafts.spec.ts`, latest run output (`3 passed, 1 failed` live guard suite), manual probe `GET http://localhost:5221/v1/audits/newsletter-template?divisionId=8 -> 404` | Claude | Open |
| DEF-0010 | 2026-04-16 | High | Report Composer runtime/editor | Composer narrative editor fails to mount due invalid Tiptap import, breaking composer gate and print/export validation | 1. Set `PW_AUDIT_COMPOSER_GATE=true` 2. Run `cmd /c npx playwright test tests/e2e/audit-report-composer-contract.spec.ts` 3. Open composer and load draft `42` | Narrative block editor renders and report composer contract tests proceed without runtime module errors | Runtime errors: `The requested module '/node_modules/.vite/deps/@tiptap_extension-text-style.js' does not provide an export named 'default'`; narrative editor locator missing; print/export assertions fail downstream | `webapp/tests/e2e/audit-report-composer-contract.spec.ts` run output + `webapp/test-results/artifacts/audit-report-composer-cont-ebc2a-ith-authored-content-intact-chromium/error-context.md` | Claude | Open |
| DEF-0011 | 2026-04-16 | Medium | Incident management - delete workflow | New incident cleanup delete in core form flow does not remove row after confirmation | 1. Run `npm run qa:baseline` 2. Observe `tests/e2e/incident-form.spec.ts` failure in submit/delete flow 3. Confirm row with generated `jobTag` still present | After delete confirmation, incident row should be removed from list (`count=0`) | Row remains (`count=1`) after delete action and confirm dialog, causing core test failure | `webapp/tests/e2e/incident-form.spec.ts:137` failure output from `qa:baseline` run on 2026-04-16 | Claude | Open |
| DEF-0012 | 2026-04-16 | Medium | QA gate scripts / Windows command portability | Reporting/composer gate scripts were using Unix-style inline env assignment and did not run on Windows shells | 1. Run prior `npm run test:e2e:audit:reporting-gate` on Windows 2. Observe command failure before tests execute | Gate scripts should execute on Windows using externally provided env vars | Command failed with `'PW_AUDIT_REPORTING_GATE' is not recognized...`; tests never started | `webapp/package.json` script diff + `Scripts/qa/Invoke-QAGate.ps1` env export update | QA | Closed |

## Severity Guidance
- High: blocks core workflow, data integrity risk, or release blocker.
- Medium: significant behavior issue with workaround.
- Low: cosmetic or minor usability issue.
